let names = ["Arouf", "Gangsta", "Triple", "Monstre"];

const afficherValeurs = (monTableau) => {
  for (let i = 0; i < monTableau.length; i++) {
    console.log(monTableau[i]);
  }
};

afficherValeurs(names);

// toto
// tata
// titi
// = []
